query GetTrash($token: String!) {
    getTrash(token: $token) {
        strategies {
            id
            deleted_at
        }
        errors {
            field
            message
        }
    }
}