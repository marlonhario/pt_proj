import React, { useState, useEffect } from "react";
import { FormGroup, Input } from "reactstrap";
export const PropertySignal = ({}): any => {
  return (
    <FormGroup>
      <Input type="select" name="signal"></Input>
    </FormGroup>
  );
};
